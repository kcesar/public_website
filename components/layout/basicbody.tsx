// @ts-ignore don't care about implicit any for now
export default function BasicBody({ children }) {
  return (
    <div className="container flex flex-col items-center px-8">{children}</div>
  );
}
